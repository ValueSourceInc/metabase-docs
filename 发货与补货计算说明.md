# 发货与补货计算说明（大白话版）

---

## 一、先理清数据链路：一张图看懂谁喂谁

```
#806 库存表(含NARF修正) ─┐
#877 发货基础表  ──┐
#876 销量预测(按天) ─┤
#899/#900 在途货件  ─┤
                   ▼
        #878 备货发货 Forecast WOS Summary   ← 真正干活的"发动机"（原生SQL，递归CTE分配）
                   │
                   ▼
        #873 补货 FBA Total Inventory Chart 2.0   ← 878 套了一层壳，字段全来自878
                   │
                   ▼
        #901 工厂补货表2.0   ← 在873基础上，按SKU汇总US/CA，再算"要生产多少"
                   ▲
                   │
        #1085 当前有效工厂补货计划   ← 只取有效期内、优先级最高的一条
                   ▲
                   │
        #1084 Wps Factory Replenish Plan   ← 运营填写的原始计划表
```

一句话：**878 算"要发多少货"（need_to_ship、allocated_qty），901 在它之上算"要生产多少"。** #1084/#1085 允许运营在有效期内按指定量或倍率调整生产建议。 873 只是把 878 的结果摆出来给人看，本身不做计算。

---

## 二、几个必须先懂的核心概念

| 概念                       | 字段                             | 大白话                                                                                                      |
| -------------------------- | -------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| **WOS（Weeks of Supply）** | `wos_now`                        | 当前库存还能卖多少周。断货天数 ÷ 7。算不出来就兜底 100 周（≈2年，因为预测只覆盖未来 700 天）。              |
| **断货天数**               | `stockout_days`                  | 按预测销量一天天扣库存，扣到不够卖的那一天，距今还有几天。                                                  |
| **在途异常库存**           | `abnormal_inventory_*`           | 在路上但状态不对（delayed 延误 / lost 丢失 / return_ship 退回）的货。这部分**不算可用库存**，因为指望不上。 |
| **退货在途量**             | `return_ship_quantity`           | 状态是 return_ship 的那部分，会退回来，**未来能重新卖**，所以单独拎出来。                                   |
| **安全库存周数**           | `safety_wos`（默认 30/7≈4.3 周） | 到货后想至少再撑这么久的安全库存。`safety_days` = safety_wos × 7。                                          |
| **发货在途天数**           | `shipping_days`（默认 30 天）    | 这次发货从工厂到 FBA 大约要多少天。                                                                         |
| **整箱规格**               | `casepack`                       | 一个整箱装几件。发货/生产都要凑整箱。                                                                       |
| **工厂库存**               | `factory_in_stock`               | 工厂仓库里这个 SKU 现有存货量。发货分配的硬上限——不能发出工厂没有的货。                                     |
| **幻影库存**               | `mirror_flag`                    | 加拿大库存表里镜像了美国库存的假库存行（NARF 计划的副作用）。不修掉会让补货系统以为 CA 还有货。              |
| **修正库存**               | `adjusted_*` 列（#806）          | 过了 NARF 安检之后的库存数字。**整个发货/补货链路用的都是这一套**，不是 MYI 原始值。                         |

---

## 三、进链路之前：#806 库存表的 NARF 幻影库存修正（完整逻辑）

补货链路的第一口数据是库存。但**加拿大站的库存表里有"幻影"**：很多本地早就没货的 SKU，在加拿大库存表里仍然显示着一行库存——那其实不是加拿大的货，而是美国库存的倒影。#806「库存表」负责在数据进链路之前把真假分清。#877 及下游看到的所有库存数字，都是修正后的 `adjusted_*` 口径，不是库存表的原始值。

**一句话总结整个方案**：用三个证据源交叉判断每个加拿大库存行是"真货"还是"美国库存的倒影"，是倒影就把数字修正掉，是真货就原样保留，证据不足时宁可不动。

### 1. 问题背景：NARF 计划与镜像行

Amazon 的北美远程履约计划（NARF）：美国仓的货，加拿大买家也能买，由美国仓直接发货给加拿大买家。好处是多一个市场的销量，坏处是**亚马逊会把美国的库存数字"抄"一份到加拿大的库存报表里**（因为加拿大买家确实能买到这批货）。

于是同一个 SKU 在库存表里有两行：美国行（真）+ 加拿大行（可能是抄本）。这个抄本就是**镜像行**。

Amazon 自己在远程履约资格报表的备注里写明了规则：

> "同一 SKU，本地 FBA 和远程履约不能共存。本地库存卖光后，才能开远程履约。"

这句话反过来读，就是判真假的钥匙：

> **加拿大本地还有货 -> 加拿大行是真的；加拿大本地卖光了 -> 加拿大行是美国库存的倒影（幻影）。**

不修的后果：补货系统看到加拿大"还有货" -> 少发货、少生产；等发现时已经断货。反过来，如果把真货误当幻影清掉，会多发货、多生产，同样浪费。

### 2. 三个数据源是什么

修正逻辑交叉使用了三个数据源，先解释清楚每一个：

**① 库存报表（MYI）**——就是 #806 的主体数据。亚马逊的全量库存清单，每个 SKU × 每个站点一行，包含仓库库存、可售、预留、在途等各类数量列。**它就是"被污染"的那个表**：镜像行的幻影数字藏在这里。

**② 远程履约资格报表（remote 表）**——亚马逊为 NARF 计划生成的资格清单，每个 SKU 一行，告诉你它有没有资格开远程履约。关键在它的列结构：**每个目标国家一组状态列**，一组就是两列——`Offer status`（状态：Enabled 已开通 / Action required 需操作 / Not eligible 不合格）+ `More Details`（备注：不合格的原因，比如"你有本地 FBA 货，和远程互斥"）。加拿大、墨西哥、巴西各一组，三组列名完全相同，导入数据库时只能按出现位置加后缀区分（`Offer status`、`Offer status_2`、`Offer status_3`）。

> **坑：两店的列顺序不一样**。DH 店的报表里加拿大是第 1 组，VS 店的报表里加拿大是第 2 组（第 1 组是墨西哥）。原因是亚马逊按卖家账号决定列顺序，两份文件的位置对不上。#806 不写死店名，每次运行**自动定位**：拿"台账确认有本地加拿大库存的 SKU"去对照三组备注列，哪组出现"local FBA"（本地 FBA 互斥）文本最多，哪组就是加拿大组。定位不到信号时全部走"不修"分支（宁可不清、不会误杀）。想审计定位结果，单独跑 SQL 里的 `v_grp` 段。

**③ 库存台账（ledger）**——亚马逊的 Ledger Summary 报表，相当于每个 SKU 的**库存流水账**：仓库里还有货的 SKU，**每天都有一行结存记录**（"Ending Warehouse Balance"，期末结存）；本地货卖光之后，行就消失。我们只看加拿大站点（Location=CA）、可售状态（SELLABLE）的行。

> **什么叫"台账日期新鲜（距全局最新日 ≤1 天）"**：每家店的台账数据整体有一个最新日期（比如 DH 同步到了 8 月 18 日）。判断某 SKU 本地有没有货时，取它在台账里的**最后一行**，但这行的日期必须落在这家店台账整体最新日的 1 天以内才算数。为什么必须查日期：台账的规律是"有货天天有行，卖光行消失"。如果某 SKU 的最后一行停在 4 月 1 日，说明它 4 月 1 日之后就没行了 = **那天之后本地就卖光了**。不查日期的话，会把 4 月的旧结存当成"现在还有货"，把镜像行误判成真货。真实案例：BT-Leaves-BU-TH 的台账结存停在 4 月 1 日（写着还有 31 件），实际本地货 4 月就卖光了，21 件幻影因此漏网——加上日期闸门后才抓出来。

### 3. 判定流程：每个加拿大行过四道关

对库存表里的每个加拿大行（SKU 以 amzn 开头的直接排除，那是亚马逊自营货），按顺序判定，结论写进 `mirror_flag` 列：

```
第 0 关：这行是美国站点吗？
   └─ 是 -> US_MARKETPLACE，原样透传，不用修（美国行本来就是真的）
第 1 关：remote 表里这个 SKU 的加拿大状态是？
   ├─ Not eligible 且备注是"你有本地 FBA 货"（互斥）-> 本地货阵营
   │      -> NOT_WHITELISTED，整行原样保留（它就是真货）
   ├─ Not eligible 但备注是别的原因（如"货源不可售/SKU 不匹配"，实际是**无法评估**）
   │      且加美仓库量、可售量**都完全相等** + 台账无新鲜结存 + 美国侧有货
   │      -> MIRROR_NOT_ELIGIBLE，按镜像修正（详见第 5 节案例 5）
   ├─ Enabled / Action required（远程已开或可开）-> 继续
   └─ remote 表里没有这个 SKU -> 也按"不动"处理
第 2 关：台账里这个 SKU 的加拿大结存 > 0 吗？（且台账日期新鲜）
   ├─ 是 -> LOCAL_STOCK，本地真实库存，原样保留
   └─ 否（无行 / 结存为 0 / 台账日期太旧）-> 是镜像行，继续
第 3 关：镜像行细分
   ├─ 加拿大仓库量 == 美国仓库量 -> MIRROR（完全镜像）
   └─ 不相等 -> MIRROR_LAG（镜像但两边还没同步完，可能是美国刚动了货）
```

为什么不能只用一条证据？单看"加拿大 == 美国"（可售量相等），138 个相等的行里有 9 个其实是本地真货恰好相等；收紧到"仓库量+可售量双相等"也还有 2 个真货。把 remote 状态、台账结存、镜像签名三层证据叠上去，误杀为零。

### 4. 判定之后：各类列的修正规则

分三种情况（只对 MIRROR / MIRROR_LAG / MIRROR_NOT_ELIGIBLE 行做修正；LOCAL_STOCK 和 NOT_WHITELISTED 基本保留原值）：

**库存列（12 个）**：仓库量、可售量、不可售、预留、调研中、调拨中、未来供应量等所有"已经在仓"的数量列。

```
修正值 = GREATEST(加拿大原始值 − 美国值, 0)    ← 减完不许为负
```

- 标准镜像：加拿大抄的是美国现值，减完自然 = 0（幻影消失）；
- 两边短暂不同步：减出的残差就是过期数字，也消掉了；万一真是加拿大独有的货，残差也兜住了；
- 加拿大比美国少（美国刚补了新货、抄本还没跟上）：减出负数，压回 0，不会出现负库存。

**在途列（3 个）**：inbound working / shipped / receiving，"还在路上"的货。修正值和库存列**同一条公式**：GREATEST(加拿大原始值 − 美国值, 0)。原因：**加拿大行的在途数 = 美国镜像的抄本 + 真实发往加拿大的在途**。亚马逊镜像时连美国的在途一起抄过来，但业务真发往加拿大的货是单列的真实增量，叠在加拿大行上。所以减法减掉的正是美国抄本，**留下的恰好是"真有货在路上"的那部分**。

实证（2026-08-20 用业务出货表逐行对账 118 个镜像行）：

- **116 个行的"CA−US 在途差"与出货表（wps_shipment_items，region=CA、未签收）的在途量分毫不差**。例：QS001-K-ST-PK 加拿大在途 456 − 美国 282 = **174**，出货表里恰好有两票未到的 CA 出货 156 + 18 = 174；历史收货也吻合（出货 60 ↔ 台账收货 60）。
- 台账证实镜像 SKU 一直在收加拿大的货（近两个半月 9 个 SKU 共 342 件），"货从未到过加拿大"的旧判断不成立。
- 若把在途清零，会误删约 **4,200 件真实在途**，补货会把这些货当成不存在而重复下单。

> 修正史：此规则曾误写成"清零"（当时误判在途为冻结旧账），2026-08-20 经业务出货表对账推翻，v8 版改回减法。

**LOCAL_STOCK 的钳制**：仓库量和可售量取 `LEAST(库存表值, 台账结存)`——两个证据取更保守的那个，防止库存表和台账轻微不同步时高估。

**总量的重算**：亚马逊的 `afn-total-quantity` 恒等于"仓库量 + 在途三列"（772/772 行验证过，其他列都是仓库量的分类子集不是加数）。所以修正后的总量 = 修正仓库量 + 修正在途三列，逐成分重算。`shipped_total` / `sellable_shipped_total`（发货总量/可售发货量）也从这个修正口径重新加总。

### 5. 四个真实案例（数字都是库存表里的实际值）

**案例 1：QS001-Q-WH（DH，标准镜像）**

| | 加拿大行 | 美国行 |
|---|---|---|
| 仓库量 / 可售量 | 238 / 231 | 238 / 231 |
| 在途已发货 | 138 | 0 |
| 判定 | **MIRROR** | US_MARKETPLACE |

- remote 状态 Enabled（远程开通），台账无结存 -> 镜像。
- 修正：仓库量 238−238=0（幻影消失）；在途 138−0=**138 保留**--这是真实发往加拿大的在途货。**修正后加拿大 = 0 件在仓 + 138 件在途**（原始总量里，仓库那 238 是抄美国的幻影，在途 138 是真的）。

**案例 2：DH-BT-BRAID-LGN-Q（DH，本地真货）**

| | 加拿大行 | 美国行 |
|---|---|---|
| 仓库量 / 可售量 | 33 / 33 | 82 / 71 |
| 台账结存 | 33 | - |
| 判定 | **NOT_WHITELISTED** | US_MARKETPLACE |

- remote 状态 Not eligible，备注是"你有本地 FBA 货，和远程互斥" -> 本地货阵营。
- 修正：**一个数都不动**（33 件是加拿大仓库里真实的货，虽然比美国少）。这就是"误杀零容忍"要保护的行。

**案例 3：QS001-Q-ST-IV（DH，本地真货 + 台账钳制）**

| | 加拿大行 | 美国行 |
|---|---|---|
| 仓库量 / 在途 | 7 / 438 | 5 / 264 |
| 台账最新结存 | 2 | - |
| 判定 | **LOCAL_STOCK** | US_MARKETPLACE |

- remote 状态 Enabled，但台账显示加拿大本地结存 2 件 -> 本地真货。
- 修正：仓库量取 LEAST(7, 2) = **2**（以更保守的台账为准）；在途 438 件是真实发往加拿大的货，**保留**。修正后总量 = 2 + 438 = 440。

**案例 4：BT-Leaves-BU-TH（VS，台账过期抓出来的漏网镜像）**

| | 加拿大行 | 美国行 |
|---|---|---|
| 仓库量 / 可售量 | 21 / 0 | 21 / 0 |
| 在途已发货 | 16 | 0 |
| 台账 | 结存 31，但日期停在 **4 月 1 日**（店全局最新日是 8 月 17 日） | - |
| 判定 | **MIRROR**（若无日期闸门会误判 LOCAL_STOCK） | US_MARKETPLACE |

- 这个 SKU 4 月本地就卖光了（台账行消失前的最后结存 31 是当时的余量）。本地清空后亚马逊翻开了远程，加拿大行开始镜像美国（21 == 21）。
- 修正：仓库量 21−21=0；在途 16−0=16 按减法保留（VS 店的出货记录不在业务出货表里、无法对账，按规则保留）。**这个案例就是"台账日期新鲜度"闸门存在的理由**--闸门管的是"仓库里还有没有货"，和在途无关。

**案例 5：DH-BT-LEAF-GN-Q（DH，remote 说 Not eligible 的镜像）**

| | 加拿大行 | 美国行 |
|---|---|---|
| 仓库量 / 可售量 | 6 / 0 | 6 / 0 |
| 在途已发货 | 42 | 18 |
| 台账 | **一行都没有**（8 个月从未有加拿大本地货） | - |
| remote 状态 | **Not eligible**，备注是"货源不可售/SKU 不匹配"（不是本地 FBA 互斥） | - |
| 业务出货表 | 2026-08-17 提货 24 件发往加拿大，ETA 10-01 | - |
| 判定 | **MIRROR_NOT_ELIGIBLE** | US_MARKETPLACE |

- 这个系列（BT-LEAF 共 40 个 SKU）2026 年 7 月才上架，业务 8 月刚发了一整票去加拿大（每 SKU 24 件，还在海上）。货没到，本地台账自然是零；美国自己的断货+SKU 命名差异让 remote 报告显示 Not eligible——**这个"不合格"是"无法评估"，不是"本地有货"**。
- 但库存数字暴露了真相：加美仓库量完全相等（6==6），加拿大在途 42 = 美国镜像 18 + 真实 CA 在途 24（和出货表分毫不差）。
- 修正：仓库量 6−6=0；在途 42−18=**24 保留**。修正后加拿大 = 24 件真实在途。
- 同系列里 remote 显示 Enabled 的 25 个走正常 MIRROR 分支，这 14 个 Not eligible 的走 MIRROR_NOT_ELIGIBLE，修法一致。留了一个 DH-BT-LEAF-RD-Q 没修（加美两边都是 0/0，空洞相等无法确认镜像，宁可漏修不误杀）。

### 6. mirror_flag 速查字典

| flag | 含义 | 数值处理 |
|---|---|---|
| `US_MARKETPLACE` | 美国站点行 | 原样透传 |
| `LOCAL_STOCK` | 加拿大本地真实库存（remote 开通 + 台账有新鲜结存） | 仓库/可售取 LEAST 钳制，在途保留 |
| `NOT_WHITELISTED` | 本地货阵营（remote 不合格，多为本地 FBA 互斥） | 原样保留 |
| `MIRROR` | 完全镜像（远程开通 + 无本地货 + 加美仓库量相等） | 库存列和在途列都用 CA−US |
| `MIRROR_LAG` | 镜像但两边未同步完 | 同 MIRROR |
| `MIRROR_NOT_ELIGIBLE` | remote 显示 Not eligible 但实为镜像（备注非"本地 FBA 互斥"，加美数值完全相等 + 台账无货） | 同 MIRROR |
| `FFQ_EQ_COINCIDENCE` | 可售量恰好相等但无其他证据 | 仅打标，不动 |
| `NO_US_ROW` | 加拿大有行但美国无行（边缘情况） | 不动 |
| `EXCLUDED_AMZN` | amzn 开头的亚马逊自营 SKU | 按设计排除 |

### 7. 修正量级与下游传导

当日快照（2026-08-20）：323 个加拿大行参与判定，其中：

- **132 个镜像行被修正**（MIRROR 113 + MIRROR_LAG 5 + MIRROR_NOT_ELIGIBLE 14）：可售库存 2,301 件 -> 1（幻影清干净），总库存 12,076 件 -> 4,668（= 真实发往 CA 的在途 4,598 + 在产 66 + MIRROR_LAG 未同步残差 4）；
- 169 个 NOT_WHITELISTED + 4 个 LOCAL_STOCK + 4 个 FFQ_EQ_COINCIDENCE 的本地真货**零误伤**（绝大多数有台账结存佐证）；
- 138 个"加美可售量相等"的行里 129 个是镜像（含 MIRROR_NOT_ELIGIBLE 14 个）、9 个是真货恰好相等——都被正确区分；127 个"仓库量+可售量双相等"的行里 2 个真货全靠台账证据捞回。

下游传导是自动的：#877 发货基础表的聚合源已经切到 `adjusted_*` 列，所以修正顺着 878（备货发货）-> 873（补货）-> 901（工厂补货）-> 930/960/985 全链路生效。方向上，873 的 need_to_ship 和 901 的 ca_need_to_produce 在修正后**整体上升**——之前幻影库存压低了真实缺口，修正后缺口显现，属于预期效果。

### 8. 维护者注意（非显而易见的坑）

1. **remote 报表列含义按店不同**：根因是亚马逊按账号决定各组列顺序，导入器按位置加 `_2`/`_3` 后缀，两份文件位置对不上。#806 已内置自动定位（见第 2 节），不写死店名，亚马逊换列序会自动跟上；定位不到信号时全部走"不修"分支；**两组 local FBA 计数并列也视为定位不到**（选组要求严格大于其他组，防止未来墨西哥铺本地 FBA 后打平时静默选错组）。
2. **#806 是原生 SQL 模型**：修正逻辑全部写在这里，MYI 原始表不加字段不动数（只进不出）。remote 数据通过 `{{#1108}}` 引用 model（Reports Get Remote Fulfillment Eligibility Model），在 1108 上改定义会自动传导。回滚用 `.backups/` 里的备份 JSON/SQL。
3. **在途列必须是减法（CA−US），不要改回清零**：加拿大行在途 = 美国镜像抄本 + 真实发往 CA 的在途（2026-08-20 用出货表对账 118 个镜像行，116 个差值分毫不差，见第 4 节实证）。清零会误删约 4,200 件真实在途。要改先重跑那次的对账验证。
4. **补货前做三表新鲜度自检**：806 的判定假设三张表时间上大体对齐。每次补货（周一）用数据前查一次：
   - remote 表 `max(created_at)` 不应比 MYI 快照旧超过约 1 天（**remote 目前没有 job、靠手工跑**，忘了重跑是最常见的坑；重跑要进同一张表，1108/806 才读得到）；
   - ledger 各店全局最新日不应比 MYI 快照旧超过约 2 天（亚马逊次日发布、周末数据拖到周二属正常；断更多半是 job 挂了）；
   - 自检不过先补拉再用。806 对坏数据的反应是**漏修而非错杀**（保守方向），但漏修的幻影会直接灌进补货计算，宁可重拉。

---

## 四、#873：要发多少货？

### 1. `need_to_ship` -- "理论上该发多少"

**核心思路：算到货那一刻会不会有缺口。**

按这个顺序推演（针对每个 SKU × 店铺 × 站点 US/CA）：

1. **今天有多少可用库存** = 当前库存 − 在途异常库存（延误/丢失/退回的那批指望不上）。
2. **发货在路上这几天还会卖掉多少** = `demand_until_arrival`（从今天到到货日，这 shipping_days 天的预测销量之和）。
3. **到货时预计还剩多少库存** = 当前库存 − 路上卖掉的 − 到货时仍是异常的库存。
4. **到货后想再撑 safety_days 天（安全库存期）**，这期间还会卖掉一批。
5. **缺口 = 到货后安全库存期内，累计需求 − 当时可用库存 的最大值**（`max_arrival_safety_deficit`）。

```
need_to_ship = 向上取整( max(0, 到货后安全库存期的缺口) )
```

> **为什么这么做？** 不是简单看"现在缺多少"，而是看"货到了之后、再过完安全库存期，会不会还是不够"。这样发出去的量，能保证货到岸后还能安稳撑过安全库存期，不会刚到货又快断货。

`need_to_ship_with_casepack` 就是把上面这个数再**向上凑成整箱的倍数**（casepack）。

---

### 2. `allocated_qty` -- "结合实际情况，真实准备发的量"

`need_to_ship` 是"按需全给"，但现实中受**三个约束**同时限制：

| 约束 | 跟踪维度 | 说明 |
| --- | --- | --- |
| **工厂库存** `factory_in_stock` | SKU × 店铺 | 能发的量不能超过工厂现有库存。US/CA 共用同一个工厂库存池——谁排在前面谁先用。 |
| **发货上限** `shipment_cap`（默认 750） | parent_asin × 站点 | 每个 parent_asin × Marketplace 一票最多发 750 件。 |
| **需求量** | SKU × 店铺 × 站点 | 分到的量不超过该 SKU 的需求（rescue_qty 或 need_qty）。 |

`allocated_qty` 就是在这三个约束下，按优先级排队分配出来的真实发货量。

> **分组维度**：发货上限按 `parent_asin_code × Marketplace` 分组。例如 parent_asin = "DH-BT-BRAID"，则 US 站点单独一批 750、CA 站点单独一批 750，互不挤占。查不到 parent_asin_code 的 SKU 归到 NULL 组，也独立分一批。工厂库存则按 SKU × 店铺共享，跨 US/CA 共一个池子。

**分配方式：逐行递归处理，三个约束同时生效。**

把所有 SKU 的请求排成一队，按顺序逐个处理。每处理一个，立即更新三个状态（已用工厂库存、已用发货额度、已分配量），下一个 SKU 看到的是**更新后的剩余额度**。任何一个约束没用完的部分（包括凑整箱丢掉的零头），立刻释放给后续 SKU。

**排队顺序：两阶段，按"谁快断货谁优先"**

**阶段一（救急 rescue）：** 先处理所有 SKU 的救急需求（`rescue_qty`）。

- 救急目标 = `safety_days × safety_wos_ratio`（`safety_wos_ratio` 默认 **1.0**，即补到完整安全库存期）。
- 按 `wos_now` 从小到大排（越快断货越靠前）。

**阶段二（补足 need）：** 救急完后，继续处理剩余需求（`need_qty − 已分配量`）。

- 同样按 `wos_now` 排序，贪心补足，直到额度用完。

**每一步的计算：**

```
本步分配量 = 向下凑整箱( min(
    剩余需求       (target_qty − 该SKU已分配量),
    剩余工厂库存   (factory_in_stock − 该SKU已用库存),
    剩余发货额度   (shipment_cap − 该组已用额度)
))
```

> **关键约束：`allocated_qty` 永远 ≤ `need_to_ship` 且 ≤ `factory_in_stock`。** 每个 SKU 分到的量不会超过它的需求，也不会超过工厂库存。
>
> **为什么这么做？**
>
> - **工厂库存封顶**：不能发出工厂没有的货。工厂库存按 SKU × 店铺共享，US/CA 谁排在前面谁先用——最紧迫的站点优先拿到库存。
> - **按 parent×站点 设 750 件上限**：物流按 parent 发货，每个 parent 的 US/CA 各发一票，互不影响。
> - **先救急再补足**：最快断货的优先保命，剩下的额度再按紧迫度补，避免"额度被慢断货的吃掉、快断货的反而没发"。
> - **救急目标用 safety_wos 的比例**：`safety_wos_ratio` 是 0~1 的小数，表示救急要补到 safety_wos 的百分之多少。永远 ≤ safety_wos，不会出现"救急量超过安全库存"的无解情况。默认 1.0 = 补到完整安全库存期（等价于全量按需分配）；调小（如 0.5）= 救急只补一半，留更多额度给阶段二按 wos 紧迫度排队分配。
> - **每步凑整箱**：物流按箱走，零头发了麻烦，宁可少发几件也要整箱。每步凑整箱后丢掉的零头会释放给后续 SKU，不会浪费。
> - 两个参数可调：`shipment_cap`（每 parent×站点上限）、`safety_wos_ratio`（救急目标占 safety_wos 的比例），改这两个值就能调分配策略。

---

## 五、#901：要生产多少？

901 把 873 按 SKU 汇总，把 US、CA 两个站点的需求并排，再扣掉工厂已有的库存，最后算出"工厂要生产多少"。

> **字段取值约定**：`active_product_num_days`（生产周期）、工厂库存 `stock`、`moq` 这三个量都是 **US 优先、US 为 0 才取 CA**（`CASE WHEN us≠0 THEN us WHEN ca≠0 THEN ca ELSE 0`）--同一个 SKU 的 US/CA 共享一套配置，优先用 US 的；而 `need_to_ship`、日销则各站点独立。

### 1. `us_need_to_produce` / `ca_need_to_produce` -- "每个站点要生产多少"

```
需要生产量 = 需要发货量(need_to_ship)
           + (生产周期 + 工厂安全库存期的一半) × 日销量
```

精确点：

```
us_need_to_produce = 向上取整(
    us_need_to_ship
  + ( active_product_num_days          ← 生产周期(天)
      + factory_safety_wos × 7 ÷ 2     ← 工厂安全库存1周，按半周期 3.5 天 折算
    ) × us_estimated_daily_sales        ← 近期平均日销
)
```

> **为什么这么做？** 发出去的货（`need_to_ship`）只是这一票要补的。但从**下单生产到货发出去**这段生产周期里，库存还在继续卖；同时工厂本身也要留一点安全库存。所以除了"要发的量"，还得把"生产这段时间还会卖掉的量"一起生产出来，否则下次又断货。工厂安全库存按半周期（1 周的一半 ≈ 3.5 天）折算，是补货节奏的约定。

### 2. `total_need_to_produce` -- "工厂总共要生产多少"

US 和 CA 各自的要生产量加起来，再扣掉**工厂现有库存**和**退货在途量**（退回来的货能重新用，省得重新生产）：

```
total_need_to_produce = 向上取整( max(0,
    us_need_to_produce + ca_need_to_produce
    - 工厂现有库存(stock)
    - 退货在途量(return_ship_quantity)
))
```

> **为什么扣这两项？**
>
> - **扣工厂库存**：工厂仓库里还有的货，不用重新生产，直接抵掉。
> - **扣退货在途**：状态是 return_ship 的货会退回来、能再卖，相当于"快到的库存"，提前抵扣，避免重复生产。
> - **max(0, ...)**：扣完若是负数（库存很充足），就归零，不生产。

### 3. `total_need_to_produce_with_moq` -- "最终下单量（考虑最小起订量）"

工厂有**最小起订量 MOQ**（一次最少做多少）。所以最终下单量不是"需要多少就生产多少"，而是按 MOQ 规则取整：

```
如果 total_need_to_produce ≥ MOQ          -> 按需生产（需要多少产多少）
如果 total_need_to_produce ≥ MOQ 的 70%    -> 按 MOQ 生产（差得不多，凑个起订量）
否则                                        -> 不生产（需求太小，不够起订量就不下单）
```

> **为什么要这条 MOQ 规则？** 工厂不愿意为了几十件开机。需求达到 7 成起订量，就值得凑整到 MOQ 下一单；连 7 成都不到，说明这个 SKU 暂时不急，干脆这轮不下单，等下一轮攒够了再说，避免零碎小单。`total_need_to_produce_with_moq` 就是业务最终看的"这单下多少"。

### 4. `final_adjusted_need_to_produce` -- "应用人工计划后的生产需求"

#901 会从 #1085 读取当前有效的工厂补货计划。#1085 基于 #1084 自动筛选：

1. 只保留 `start_date <= 今天 <= end_date` 的计划。
2. 同一个 `sku + store` 有多条有效计划时，`priority` 最大的生效。
3. `priority` 相同时，最后新增的记录（`_wps_id` 最大）生效。

最终生产需求按以下优先级计算：

```
如果 multiplier = 0
    final_adjusted_need_to_produce = 0
否则如果 specified_quantity 不为空
    final_adjusted_need_to_produce = max(0, specified_quantity)
否则如果 multiplier 不为空
    final_adjusted_need_to_produce = max(0, total_need_to_produce × multiplier)
否则
    final_adjusted_need_to_produce = total_need_to_produce
```

`multiplier=0` 是最高优先级的"本期停产"开关，即使同时填了 `specified_quantity` 也不生产。`multiplier` 不为 0 时，`specified_quantity` 是绝对指定量，填写 `0` 也表示本期不生产；指定量和非零倍率同时填写时，指定量优先。

### 5. `final_adjusted_need_to_produce_with_moq` -- "应用计划后的最终下单量"

这个字段才是应用人工计划并重新判断 MOQ 后的最终下单建议：

```
如果 final_adjusted_need_to_produce ≥ MOQ          -> 按 final_adjusted_need_to_produce 下单
如果 final_adjusted_need_to_produce ≥ MOQ 的 70%   -> 按 MOQ 下单
否则                                                -> 0，本期不下单
```

> `total_need_to_produce*` 是系统原始建议，`final_adjusted_need_to_produce` 和 `final_adjusted_need_to_produce_with_moq` 是应用当前补货计划后的结果。实际下单优先看 `final_adjusted_need_to_produce_with_moq`。

---

## 六、算一遍：一个 SKU 的完整计算过程

下面用一个虚拟 SKU **"QS001-T-ST-DG"** 把全链路算一遍。数字是编的，只为讲清逻辑，不代表真实数据。

**前提参数（该 SKU 在 US 站点）：**

| 项                           | 值                            | 说明                                        |
| ---------------------------- | ----------------------------- | ------------------------------------------- |
| 当前可用库存                 | 205 件                        | 今天 FBA 能卖的库存                         |
| 在途异常库存（到货时仍异常） | 20 件                         | 延误/丢失，指望不上                         |
| 预测日销                     | 5 件/天                       | `estimated_daily_sales_after_shipping_days` |
| 发货在途天数 `shipping_days` | 30 天                         | 工厂发到 FBA 要 30 天                       |
| 安全库存 `safety_wos`        | 30/7 周 -> `safety_days`=30 天 | 到货后想再撑 30 天                          |
| 整箱规格 `casepack`          | 12 件                         | 一箱 12 件                                  |
| 工厂库存 `factory_in_stock`  | 200 件                        | 工厂仓库现有存货                            |
| `wos_now`                    | 0.5 周                        | 当前只够卖 3.5 天，很急                     |

---

### 第一步：算 `need_to_ship`（理论该发多少）

**① 货在路上 30 天，会卖掉多少？**

```
demand_until_arrival = 30 天 × 5 件/天 = 150 件
```

**② 货到岸时，预计还剩多少可用库存？**

```
到货时可用库存 = 当前库存 − 路上卖掉的 − 到货时仍异常的
             = 205 − 150 − 20 = 35 件
```

**③ 到货后再过 30 天安全库存期，这期间还要卖多少？**

```
安全期销量 = 30 天 × 5 件/天 = 150 件
```

**④ 缺口 = 安全期内累计需求 − 到货时可用库存 的最大值**

随着安全期一天天过去，累计需求从 0 涨到 150，可用库存一直是 35（不再补）。缺口最大出现在安全期最后一天：

```
max_arrival_safety_deficit = 150 − 35 = 115 件
```

**⑤ `need_to_ship` = 向上取整**

```
need_to_ship = ceil(max(0, 115)) = 115 件
```

**⑥ `need_to_ship_with_casepack` = 向上凑整箱**

```
ceil(115 ÷ 12) × 12 = ceil(9.58) × 12 = 10 × 12 = 120 件
```

> 到这里：理论上该发 115 件，凑整箱后 120 件。但实际能不能发这么多，看下一步排队。

---

### 第二步：算 `allocated_qty`（实际准备发多少）

`need_to_ship` 是"按需全给"，但同一 parent × US 站点一票最多发 750 件（`shipment_cap`=750），且不能超过工厂库存。假设该 parent 的 US 站点这票有 3 个 SKU 要发，按"谁快断货谁优先"（`wos_now` 升序）排：

| 排序 | SKU               | wos_now(周) | need_to_ship | rescue_qty(补到 safety_wos×0.5) | factory_in_stock |
| ---- | ----------------- | ----------- | ------------ | ------------------------------- | ----------------- |
| 1    | **QS001（本例）** | 0.5         | 115          | 40                              | 200               |
| 2    | SKU-B             | 1.2         | 200          | 10                              | 80                |
| 3    | SKU-C             | 2.0         | 300          | 0                               | 500               |

> 这里 `safety_wos_ratio` 取 **0.5** 演示两阶段分配。rescue_qty 是「到货后前半段安全库存期（ratio×safety_days）内的缺口」，比 need_to_ship 小，因为 need 是整个安全库存期（safety_days）的累计缺口（本例 QS001：到货剩 35 件、日销 5 件，前 15 天缺口 5×15−35=40，整段 30 天缺口 5×30−35=115）。**默认 ratio=1.0 时两者相等**（救急窗口=整段安全期），阶段一即覆盖全部 need、阶段二为空；调小 ratio 才留出额度给阶段二按紧迫度排队。

> 注意 SKU-B 的 `factory_in_stock` 只有 80 件——这会成为瓶颈，下面会看到它如何影响分配。

系统把 6 个请求（3 个 SKU × 2 个阶段）排成一队，逐个处理。每步同时检查三个约束，取最小值后向下凑整箱，剩余额度立刻释放给下一个。

**阶段一 · 救急**（target = rescue_qty，按 wos_now 升序）：

| 步骤 | SKU   | target | 工厂库存剩余 | cap剩余 | raw = min(三者) | 凑整箱(÷12向下) | 实际分配 |
| ---- | ----- | ------ | ------------ | ------- | --------------- | --------------- | -------- |
| 1    | QS001 | 40     | 200          | 750     | min(40,200,750)=40 | floor(40÷12)×12=36 | **36**   |
| 2    | SKU-B | 10     | 80           | 714     | min(10,80,714)=10  | floor(10÷12)×12=0  | **0**    |
| 3    | SKU-C | 0      | 500          | 714     | 0                  | 0                  | **0**    |

阶段一后状态：QS001 已分 36（工厂用 36、cap 用 36）；SKU-B/C 均为 0。

> QS001 救急需求 40，但凑整箱后只能分 36，丢掉的 4 件零头**立刻释放**——工厂库存和 cap 额度只扣 36，不扣 40。
> SKU-B 救急需求 10，凑整箱后为 0（不够一箱），这步不分。

**阶段二 · 补足**（target = need_qty，按 wos_now 升序，扣掉阶段一已分配量）：

| 步骤 | SKU   | 剩余需求 (need−已分) | 工厂库存剩余 | cap剩余 | raw = min(三者) | 凑整箱(÷12向下) | 实际分配 |
| ---- | ----- | -------------------- | ------------ | ------- | --------------- | --------------- | -------- |
| 4    | QS001 | 115−36=79            | 200−36=164   | 750−36=714 | min(79,164,714)=79 | floor(79÷12)×12=72 | **72**   |
| 5    | SKU-B | 200−0=200            | 80−0=80      | 714−72=642 | min(200,80,642)=**80** | floor(80÷12)×12=72 | **72**   |
| 6    | SKU-C | 300−0=300            | 500−0=500    | 642−72=570 | min(300,500,570)=300 | floor(300÷12)×12=300 | **300**  |

**合计：**

| SKU   | 阶段一 | 阶段二 | `allocated_qty` | 说明                              |
| ----- | ------ | ------ | --------------- | --------------------------------- |
| QS001 | 36     | 72     | **108**         | 36+72=108，凑整箱丢了 7 件零头     |
| SKU-B | 0      | 72     | **72**          | 工厂库存只有 80，被截断后凑整箱=72 |
| SKU-C | 0      | 300    | **300**         | 满额分配，不受限                   |

> **重点看 SKU-B**：理论要发 200 件，但工厂库存只有 80 件，`min(200, 80, 642)` 取 80，凑整箱后只有 72 件。**没有工厂库存就发不了**。
>
> **再看 SKU-C**：如果 SKU-B 没被工厂库存卡住（按旧逻辑能拿 192），cap 会被消耗更多，SKU-C 拿不到满额。现在 SKU-B 只用了 72 件 cap，释放出的额度让 SKU-C 拿到完整的 300 件——这就是"逐行递归、额度即时释放"的效果。
>
> ⚠️ **注意一个关键点**：下一步算"要生产多少"用的是 `need_to_ship`（理论值 115），**不是** `allocated_qty`（实际发的 108）。也就是说，即使这票发货被工厂库存或 cap 卡住，生产量仍按"理论该发多少"来备——因为生产是为补货兜底，不是只补这一票。

---

### 第三步：算 `us_need_to_produce`（US 站点要生产多少）

```
us_need_to_produce = ceil( us_need_to_ship + (生产周期 + 工厂安全库存半周期) × 日销 )
```

代入：

| 项                                             | 值                             |
| ---------------------------------------------- | ------------------------------ |
| us_need_to_ship                                | 115 件（理论发货量，不是 108） |
| 生产周期 `active_product_num_days`             | 45 天                          |
| 工厂安全库存 `factory_safety_wos`=1 周 -> 1×7÷2 | 3.5 天（按半周期折算）         |
| US 日销                                        | 5 件/天                        |

```
= ceil( 115 + (45 + 3.5) × 5 )
= ceil( 115 + 48.5 × 5 )
= ceil( 115 + 242.5 )
= 358 件
```

> 比"要发的 115"多出来约 243 件，是因为从下单生产到货发出去这 48.5 天（45+3.5）里，库存还在以每天 5 件的速度卖，这部分也要一起生产出来，否则下次又断货。

---

### 第四步：算 `total_need_to_produce`（工厂总共要生产多少）

把 US 和 CA 两个站点的要生产量加起来，再扣工厂现有库存和退货在途。

假设 CA 站点该 SKU：`ca_need_to_ship`=50、CA 日销=3、同样生产周期 45 天：

```
ca_need_to_produce = ceil( 50 + (45+3.5) × 3 ) = ceil( 50 + 145.5 ) = 196 件
```

再假设：工厂现有库存 300 件，退货在途 20 件。

```
total_need_to_produce = ceil( max(0,
    us_need_to_produce + ca_need_to_produce − 工厂库存 − 退货在途
))
= ceil( max(0, 358 + 196 − 300 − 20) )
= ceil( 234 )
= 234 件
```

> 工厂仓里已有 300 件、还有 20 件退货要回来，能抵掉 320 件，所以净生产需求从 554 降到 234。

---

### 第五步：算 `total_need_to_produce_with_moq`（最终下单量）

设该 SKU 的 MOQ = 500 件。按 MOQ 规则判定：

```
234 ≥ 500 ?         否
234 ≥ 500 × 70% (=350) ?   否
-> 不下单，total_need_to_produce_with_moq = 0
```

本例需求 234 件，连 7 成起订量（350）都不到，这轮不下单，等下一轮攒够了再说。

**三种情况对比（同样 MOQ=500）：**

| SKU           | total_need_to_produce | 判定            | 最终下单量          |
| ------------- | --------------------- | --------------- | ------------------- |
| QS001（本例） | 234                   | < 350（7成）    | **0**（不下单）     |
| SKU-D         | 380                   | 350 ≤ 380 < 500 | **500**（凑 MOQ）   |
| SKU-E         | 620                   | ≥ 500           | **620**（按需生产） |

---

### 全链路一图回顾（QS001）

```
当前库存205 -> 到货时剩35 -> 安全期缺口115
  -> need_to_ship = 115           (理论该发)
  -> 排队+工厂库存+凑整箱 -> allocated_qty = 108   (这票实际发)
  -> us_need_to_produce = 358      (加上生产周期内的销量)
  -> +CA(196) −工厂库存(300) −退货(20)
  -> total_need_to_produce = 234   (工厂净生产需求)
  -> MOQ判定(234<350) -> with_moq = 0    (这轮不下单)
```

---

## 七、可调参数一览（业务可以动这几个旋钮）

| 参数                 | 在哪     | 默认      | 作用                                                                                                                       |
| -------------------- | -------- | --------- | -------------------------------------------------------------------------------------------------------------------------- |
| `shipment_cap`       | #878     | 750       | 每个 parent_asin × 站点 一票最多发多少件。调大->一次发更多、覆盖更全；调小->更保守、分多票。                                 |
| `safety_wos_ratio`   | #878     | 1.0       | 救急目标占 safety_wos 的比例（0~1 小数）。1.0=补到完整安全库存期；调小（如0.5）=救急只补一半，留额度给阶段二按紧迫度排队。 |
| `safety_wos`         | #877->878 | ~4.3 周   | 到货后想留的安全库存周数。调大->need_to_ship 偏大、更保险。                                                                 |
| `shipping_days`      | #877->878 | 30 天     | 发货在途天数。调大->路上卖得更多、缺口更大、要发更多。                                                                      |
| `factory_safety_wos` | #901     | 1 周      | 工厂安全库存周数（写死常量，按半周期 1×7÷2=3.5 天 折算进生产量）。                                                         |
| `moq`                | 上游     | 每SKU不同 | 最小起订量，决定最终下单量的取整门槛。                                                                                     |
| `forecast_type`       | WPS 预测表 | 空 | 填 `promotion` 标记促销记录：促销的日子不进历史日销平均。不打标签，活动结束照样污染（详见第十节）。                              |
| `recovery_daily_sales` | WPS 预测表 | 空 | 回归期日销（可选）：促销结束后 N 天按这个数预测，表达"促销后有一段淡季"。不填则促销一结束直接回到干净的历史日销。              |
| `recovery_days`       | WPS 预测表 | 空 | 回归期天数（可选），与上一行配套；只填天数不填日销则无效。                                                                  |

---

## 八、一张表速查"这个数到底怎么来的"

| 你看到的字段                              | 在哪张表 | 一句话算法                                                                                                                                      |
| ----------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `adjusted_*` / `mirror_flag`              | #806     | NARF 修正后的库存与真假标记：镜像行库存列和在途列都取 GREATEST(CA−US,0)；本地货原样保留。                                                             |
| `need_to_ship`                            | #873     | 到货后 + 安全库存期内会出现的缺口，向上取整。                                                                                                   |
| `need_to_ship_with_casepack`              | #873     | need_to_ship 向上凑整箱。                                                                                                                       |
| `allocated_qty`                           | #873     | 逐行递归分配，每步同时受三个约束（工厂库存、parent×站点750件上限、需求量），取最小值后向下凑整箱，剩余额度即时释放给后续SKU。永不超过 need_to_ship 和 factory_in_stock。 |
| `us/ca_need_to_produce`                   | #901     | 该站点要发的量 + (生产周期 + 工厂安全库存半周期) × 日销。                                                                                       |
| `total_need_to_produce`                   | #901     | US+CA 要生产量 − 工厂现有库存 − 退货在途，不够 0 算。                                                                                           |
| `total_need_to_produce_with_moq`          | #901     | 上面这个数按 MOQ 规则取整：≥MOQ 按需、≥7成MOQ 按MOQ、否则不下单。                                                                               |
| `final_adjusted_need_to_produce`          | #901     | 应用当前有效计划后的生产需求：倍率0停产，否则指定量优先或按倍率调整，最低为0。                                                                  |
| `final_adjusted_need_to_produce_with_moq` | #901     | 对 final_adjusted_need_to_produce 重新执行 70% MOQ 规则；实际下单优先看这个字段。                                                               |

---

## 九、Replenish 表使用说明（可直接发给使用者）

### 什么时候用

系统生产建议需要临时调整时使用，例如促销、清库存、暂停生产或确定必须生产某个数量。没有特殊情况时不用填写，#901 会继续使用系统原始建议。

### 怎么填

在 `wps_factory_replenish_plan`（#1084）新增一行：

| 字段                      | 怎么填                                                                                  |
| ------------------------- | --------------------------------------------------------------------------------------- |
| `sku`                     | 填准确的 SKU，必须与 #901 一致。                                                        |
| `store`                   | 填店铺代码，例如 `DH`；必须与 #901 的 `custom_store` 一致。                             |
| `start_date` / `end_date` | 计划生效的开始和结束日期，开始、结束当天都生效。                                        |
| `specified_quantity`      | 要直接指定生产数量时填写，包括 `0`；使用它时通常不填倍率。                              |
| `multiplier`              | 要按系统建议调整比例时填写，例如 `0.8` 表示 80%，`1.2` 表示 120%；填 `0` 表示本期停产。 |
| `priority`                | 多条计划日期重叠时，数字大的优先。普通计划可填 `100`。                                  |
| `description`             | 简单写原因，例如"清库存""旺季备货"。                                                    |

### 常用例子

| 目的             | specified_quantity | multiplier |
| ---------------- | -----------------: | ---------: |
| 暂停生产         |               留空 |        `0` |
| 固定生产 500 件  |              `500` |       留空 |
| 系统建议减少 20% |               留空 |      `0.8` |
| 系统建议增加 30% |               留空 |      `1.3` |

### 看哪个结果

- `total_need_to_produce_with_moq`：系统原始下单建议。
- `final_adjusted_need_to_produce`：应用计划后的需求，尚未执行 MOQ。
- `final_adjusted_need_to_produce_with_moq`：应用计划并执行 MOQ 后的最终下单建议，**实际使用看这个字段**。

### 注意事项

1. `specified_quantity` 和 `multiplier` 通常不要同时填写；`multiplier=0` 永远优先并表示停产，其他情况下同时填写则指定量优先。
2. 日期过期后计划自动失效，不需要删除历史记录。
3. 同一个 SKU、店铺、日期有多条计划时，`priority` 最大的生效；相同优先级下最后新增的生效。
4. 不要填写负数；系统会按 0 处理。
5. 新增后刷新 #901，核对 `final_adjusted_need_to_produce_with_moq` 是否符合预期。

---

## 十、促销期间与促销后怎么算（2026-08-21 新增）

### 1. 解决什么问题

以前配了促销倍率（比如 ×5 备黑五），活动结束后，活动期间的大销量会掺进"最近 30 天平均日销"，把历史日销抬高。系统以为不做促销也能天天卖那么多，发货、补货、生产全部算多。

### 2. 现在的规则：每天按三条查

| 日期落在哪 | 每天按几个卖 | 数从哪来 |
| --- | --- | --- |
| 促销期间（打了 `promotion` 标签的记录的起止日期内） | 你配的日销×倍率；没配日销/倍率就用剔除促销后的历史日销（×倍率） | #876 |
| 促销结束后 N 天（回归期） | 你配的 `recovery_daily_sales`；没配就用干净的历史日销 | #876 自动生成的回归行 |
| 其他日子 | 历史日销（最近 30 个正常日的平均） | #954 |
| 没填 end_date（一直生效） | 记录配置一直生效：配 0/天 = 停产、不再补货该站点；配其他值就按那个值一直预测。历史剔除也从开始日起一直生效 | #876 / #954 |

### 3. "干净的历史日销"怎么算：手填值代替 + 自然 30 天窗口

**口径于 2026-08-21 定稿（第二版，老板口径），取代当日早些时候的"纯剔除"版**。规则：

1. **窗口是自然日历的"昨天往前 30 天"**（不是"最近 30 个正常日"）；新品从首单日期起到昨天
2. 窗口内每一天取值：**配了 `daily_sales` 的促销日按手填值代入**（代替当天实际销量）；**只配倍率没配日销的促销日剔除**（不参与平均）；其他日子按实际销量
3. 日销 = 总值 ÷ **参与计算的天数**（日历口径，不是卖出天数）

纯剔除版的问题：新品促销期一剔除，可用天数变 0，基线跌到 0；滑动正常日窗口又把远期旧促销日圈进来。代入版让手填日销本身成为基线的种子--这正是"手填 daily_sales 就是想让它当那几天的真实销量"的本意。新增三列，旧列一个没动：

| 新列 | 含义 |
| --- | --- |
| `promo_excluded_sales_per_day` | 干净基线日销（列名沿用旧名，语义已是第二版"代入"口径：手填值代替促销日实际销量、倍率日剔除、÷日历天数） |
| `normal_days_sold` | 实际参与计算的正常销售日数 |
| `promo_day_ratio` | 最近 30 天里促销日占比 |

### 4. 回归期是可选的

回归期表达"促销后有一段淡季"（购买力被提前透支）。不填完全不生效；只填天数不填日销也无效。回归行优先级固定为 0（现有配置最小 10），所以回归期内如有正式配置（比如紧接着又一场促销），正式配置生效。

**为什么保留**：代入口径下即使删掉回归期，折线也不会跳回被污染的高基线（见第 7 节"自动恢复"），回归期只剩一个作用--**主动预报促销后的低谷**（比如 QS001-Q-BU 手填的 0.1/天）。不填 = 恰好等于老板口径，零副作用。

### 5. 改了哪些表

| 卡片 | 改动 |
| --- | --- |
| #876 | 输出加 `forecast_type` 列；promotion 记录自动展开回归行（日销=回归日销、倍率空、优先级 0）；没填 end_date 的记录展开到规划期末（+700 天），停产记录的 0/天配置得以生效 |
| #954 | 新增上面 3 列；没填 end_date 的促销记录从开始日起一直剔除 |
| #877 | 聚合末尾追加 `avg_promo_excluded_sales_per_day`（对 #954 新列求平均） |（第二版：`history_daily_sales` 外层再套 COALESCE 优先取干净基线）
| #878 | `adjusted_history_daily_sales` 的"卖出≥30天"分支改用新列（原 avg_2 兜底） |
| #873 / #901 / #880 | **零改动**，自动受益 |
| #879 | 兜底历史日销（`history_daily_sales`）的"卖出≥30天"分支改用 #877 的 `avg_promo_excluded_sales_per_day`（原 avg_2）；`daily_sales` 判空从 `NULLIF(x,0)` 改为 `IS NOT NULL`，与 #878 对齐——`daily_sales=0`（停产）现在真的输出 0，不再回落到历史日销 |

#879 只喂 #880 趋势图，不影响补货数量；2026-08-21 已同步修复（原是本节唯一还读旧口径的卡片）。实测 QS001-Q-BU（US）：未促销的普通日子 11.38 -> 0.23/天，10/11-12 黑五倍率日 56.92 -> 1.17（0.23×5）；57 个停产 SKU 的未来折线从历史日销水平压到 0。全库 160,674 行里 57,317 行数值变化（163 个 sku/站点组合：74 个基线被促销污染、57 个带停产配置、105 个带倍率配置，三类互相重叠）。

### 6. 运营侧要做的事

1. WPS 预测表里给促销记录的 `forecast_type` 填 `promotion`。黑五/十月活动已全部打完（截至 2026-08-21 剩 0 条）
2. 想要回归期就填 `recovery_daily_sales` 和 `recovery_days`
3. 没填 `end_date` = 记录一直生效；配 `daily_sales=0` 才是停产（不再补货该站点）。这是**正确用法**（2026-08-21 起 876/954 已支持）。注意记录按站点生效：只建了 US 记录就只堵 US，CA 要堵需另建 CA 记录（当前 QS001-Q-GN/QS001-CK-ST-GN/QS001-T-GN 在 #901 剩的量就是 CA 侧的）
4. 有 14 条起止日期颠倒的坏记录（id 3971~3984，DH-QS-CS001 系列，program=New）建议删除
5. 促销记录活动结束后建议保留 90 天再删（历史日销要回看剔除）

### 7. 怎么确认它工作了

看 #954：促销日过去后，`sales_per_day`（旧口径，会被抬高）和 `promo_excluded_sales_per_day`（新口径，干净）会拉开差距。

**2026-08-21 已实测一例**：QS001-Q-BU（US）8 月促销 8/8~8/22 打标后，US 基线 11.38 → 0.23/天，#878 需发货 561 → 0，#901 需生产 887 → 0（该 SKU US 可用库存 112 个、日销 0.23，本就不缺货；此前的 887 几乎全是被促销销量污染出来的）。同时全库 439 行里有 79 行带促销日标记、71 行的剔除口径与旧口径拉开差距。

**同日"一直生效"支持实测**：没填 end_date 的记录此前完全无效（在 876/954 里展开为空）。当前 64 条这类记录全部配 0/天（停产用法），此前导致 25 个停产 SKU 在 #901 还挂着生产建议（CTN_SATN 系列最高 298，霸榜前 10）。支持后剩 3 个（均为 CA 侧量，因记录只堵 US），#901 榜首变为真实在售 SKU。

测试 SKU QS001-Q-BU（CA/US，10/11~12 促销 ×5、回归期 10/13~11/11 日销 0.1）在 **2026-10-13 之后**可观察回归期效果。

### 8. 第二版口径实测（2026-08-21 定稿当天）

**验收用例（老板指定）**：DH-QS-ST002-T-OL（US）此前 #1110 折线在 10/7 之后跳回 4/天（上市首日卖了 4 个、只卖了 1 天，"÷卖出天数"口径外推出 4/天；黑五 ×5 倍率再放大到 20/天）。第二版上线后：

- 基线 4.0 -> **0.049/天**（手填配置 9/8~10/8 的 0.0327 代入 + 首单锚定窗口），10/8 之后折线保持 0.049 不再跳回 4
- 黑五倍率日 20 -> 0.245（0.049×5，倍率乘在干净基线上）
- #878 需发货 67 -> 0、分配 66 -> 0、当前库存周数 7.3 -> 50.3 周

**全库影响**：439 行里 303 行变化（90 行上调，多为配置播种的新品；213 行下调，多为稀疏 SKU--旧"÷卖出天数"口径把 30 天卖 8 个算成 1/天，现在按日历 0.27/天）。#878 需发货总量 3617 -> 1812（-50%），#901 生产建议同步收缩（9 个残余需求全部低于 MOQ 的 70% 线被清零，榜单干净）。

**两个自动恢复机制（不靠回归期字段）**：
1. 只配倍率的促销（如黑五 ×5）：倍率日本来就不进基线，活动结束折线**立即**回到干净基线，无需恢复
2. 配了日销的促销：活动结束后基线不会跳变--滚动 30 天窗口里那几天仍按手填值计，折线保持平稳，随窗口滚动在之后 30 天里逐渐向实际销量收敛

**已知残留**：完全没配置、且只卖出过首日的新品，仍会外推首日日销（如 BT-Leaves-GN-TH-XL CA 1.0/天），第 2 天起自愈。这类 SKU 建议在 WPS 表里补一条手填配置。

---


_数据口径截至 2026-08-21（NARF 修正为 2026-08-20 上线的 v10 版逻辑：状态列自动定位且并列判 0、remote 走 model 1108、ledger 新鲜度闸门、镜像行在途列按 CA−US 减法保留真实在途、MIRROR_NOT_ELIGIBLE 分支；2026-08-21 上午上线促销标记与回归期机制，同日午后按老板口径定稿第二版基线：手填 daily_sales 代入促销日、自然日历 30 天窗口、首单锚定新品、日历天数分母，见第十节）。具体公式以卡片实际逻辑为准，本文为业务理解用的白话解读。_
